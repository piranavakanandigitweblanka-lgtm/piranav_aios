
    {% comment %} CORRECT DYNAMIC HREFLANG IMPLEMENTATION FOR SHOPIFY MARKETS piranav add in 10/12/2025 {% endcomment %}
    {% comment %} {% for locale in shop.published_locales %}
      {% if locale.iso_code != request.locale.iso_code %}
        <link
          rel="alternate"
          href="{{ page.url | replace: request.locale.root_url, locale.root_url }}"
          hreflang="{{ locale.iso_code }}"
        >
      {% endif %}
    {% endfor %}
    <link
      rel="alternate"
      href="{{ shop.url }}{{ page.url }}"
      hreflang="x-default"
    > {% endcomment %}

{%- comment -%} Noindex for search pages and parameter URLs update 2026.02.05 piranav {%- endcomment -%}
{% if template contains 'search' %}
  <meta name="robots" content="noindex, follow">
{% endif %}

<script>
  (function() {
    const currentURL = new URL(window.location);
    const URLContainsSrsltid = currentURL.searchParams.get('srsltid');

    if (URLContainsSrsltid) {
      const newMetaRobotsTag = document.createElement('meta');
      newMetaRobotsTag.name = 'robots';
      newMetaRobotsTag.content = 'noindex,follow';
      document.head.appendChild(newMetaRobotsTag);
    }
  })();
</script>
